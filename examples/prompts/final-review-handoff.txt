Use the task-evidence MCP server for this long-running task.
Start a session, capture checkpoints at the major milestones, and only record when motion truly matters.
When the workflow is complete, compare the latest relevant artifacts, summarize the verdict and review focus, and return:
- the session directory
- the summary path
- the most important artifact paths
- any redacted shareable copies if they were created
